<%= form_with(model: concert) do |form| %>
  <% if concert.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(concert.errors.count, "error") %> prohibited this concert from being saved:</h2>

      <ul>
        <% concert.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :fecha, style: "display: block" %>
    <%= form.datetime_field :fecha %>
  </div>

  <div>
    <%= form.label :asistentes, style: "display: block" %>
    <%= form.number_field :asistentes %>
  </div>

  <div>
    <%= form.label :lugar, style: "display: block" %>
    <%= form.text_field :lugar %>
  </div>

  <div>
    <%= form.label :duracion, style: "display: block" %>
    <%= form.number_field :duracion %>
  </div>

  <div>
    <%= form.label :group_id, style: "display: block" %>
    <%= form.select(:group_id, options_from_collection_for_select(groups, :id, :nombre), {}, { class: "form-select" }) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
